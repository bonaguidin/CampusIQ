# Public Supabase project config. VITE_ vars are bundled into client-side
# JS and served to the browser -- never put a secret/service-role key
# here or in any frontend/.env* file. Only the URL and publishable
# (anon) key belong in frontend env.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
